<div class="nav_bar">
    <nav>

    <ul class="nav-list">
      <li><%= link_to 'Home', root_path %></li>
      <li><%= link_to 'About', about_path %></li>
      <li><%= link_to 'Teams', teams_path %></li>
      <li><%= link_to 'Players', player_performance_index_path %></li>
      <li>
        <div class="nav-search">
          <%= form_tag search_path, method: :get do %>
            <% # Added a category select to find player by the team selected %>
            <%= collection_select :team, :id, @search_categories, :id, :teamAbbrevs, { include_blank: 'All' }, { class: 'form-control' } %>
            <%= text_field_tag :query, params[:query] %>
            <%= submit_tag "Search" %>
          <% end %>
        </div>
      </li>
    </ul>

  </nav>
</div>